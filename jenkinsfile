pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building'
		#python -m pip install pytest
            }
        }
        stage('Test') {
            steps {
                sh 'python3 tese_add.py'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying'
            }
        }
    }
}

